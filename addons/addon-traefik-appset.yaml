apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addon-traefik
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - {}
  template:
    metadata:
      name: traefik
    spec:
      project: default
      source:
        repoURL: https://github.com/atidyshirt/home-server.git
        targetRevision: main
        path: addons/traefik/base
      destination:
        server: https://kubernetes.default.svc
        namespace: traefik
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
