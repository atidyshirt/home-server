apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addon-envoy-gateway
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - {}
  template:
    metadata:
      name: envoy-gateway
    spec:
      project: default
      source:
        repoURL: https://github.com/atidyshirt/home-server.git
        targetRevision: main
        path: addons/envoy-gateway/base
      destination:
        server: https://kubernetes.default.svc
        namespace: envoy-gateway-system
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
