apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - name: lan-gateway
      namespace: envoy-gateway-system
  hostnames:
    - argo.homelab.dev
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
