# Same item as applications/arc-runners/onepassword-items and
# applications/argo-workflows/base's own copy - itemPath doesn't vary,
# only the destination namespace (set by the consuming Application).
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: ghcr-push
spec:
  itemPath: "vaults/homelab/items/ghcr-push"
