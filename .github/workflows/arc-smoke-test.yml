name: ARC smoke test

on:
  workflow_dispatch: {}

jobs:
  smoke-test:
    runs-on: [self-hosted, home-server-runner-set]
    steps:
      - run: echo "ARC ephemeral runner picked up the job on $(hostname)"
